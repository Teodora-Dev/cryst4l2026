# Changelog

## Unreleased

## 2026-01-29
- Pure CSS rewrite using `@layer` (ITCSS in native CSS)
- Modular folder structure matching original Sass starter
- Fluid type + space tokens
- Container query utilities + CQ width fractions using `@*-up` suffix naming
- Structure-only core components (`c-*`)
- One-page docs site in `/docs` for GitHub Pages
